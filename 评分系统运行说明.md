# 会议资源支持评分系统运行说明

## 访问地址

- 评分录入页：http://localhost:8036/index.html
- 过往申请页：http://localhost:8036/history.html
- 服务健康检查：http://localhost:8036/api/health
- 内网同事访问：http://10.5.2.31:8036/index.html
- 内网过往申请：http://10.5.2.31:8036/history.html

## 重启后的运行方式

系统已在当前 Windows 用户的“启动”文件夹创建自启动快捷方式：

`会议资源评分系统服务.lnk`

电脑关机重启并登录当前用户后，会自动启动本地服务。启动文件夹和注册表 Run 均指向当前用户启动文件夹中的 `会议资源评分系统服务.vbs`，该脚本会直接拉起 Python 服务，避免 CMD 中文路径乱码。启动后继续使用：

`http://localhost:8036/index.html`

同一内网同事可尝试访问：

`http://10.5.2.31:8036/index.html`

如果同事无法访问，通常是 Windows 防火墙或公司网络策略阻止了 8036 端口，需要管理员或 IT 放行 TCP 入站端口 8036。

## 手动恢复

如果浏览器提示无法访问，可双击本文件夹中的：

`启动评分系统.bat`

然后重新打开访问地址。

注意：当前方案是在 Windows 用户登录后自动启动。如果电脑停在登录界面、没有用户登录，服务不会启动。若需要无人登录也启动，需要 IT/管理员把本系统注册为 Windows 服务或管理员计划任务。

## 数据保存

- 页面保存申请后，会写入浏览器本地记录。
- 同时会写入服务端统一记录：`applications.json`
- 同时会追加写入：`资源支持价值评估评分表2026版.xlsx`
- 如果 Excel 文件正在打开并占用，网页会提示同步失败；关闭 Excel 后重新保存即可。

## 内部同事同步

- 同事通过内网地址访问并保存申请后，记录会统一写入主机电脑的 `applications.json` 和 Excel。
- 所有人访问 `http://10.5.2.31:8036/history.html` 看到的是同一份过往申请记录。
- 过往申请页会自动刷新同步，无需手动导入。
