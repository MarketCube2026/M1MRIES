# 评分系统运行说明

## 推荐公网运行方式

公网共享建议采用 GitHub Pages + Supabase：

- GitHub Pages 负责打开网页。
- Supabase 负责保存、读取、删除和导出申请记录。
- 用户电脑关机、重启、本地服务关闭，都不会影响公网网页和云端数据。

### 配置步骤

1. 登录 Supabase 并新建项目。
2. 在 SQL Editor 中执行 `supabase_schema.sql`。
3. 在 Authentication 中创建内部同事账号。
4. 修改 `cloud-config.js`，填入 Supabase anon public key，并确认：

```js
enabled: true,
supabaseUrl: "https://liqbfzbrjyvccjcktaxy.supabase.co",
authRequired: true,
cloudOnly: true
```

5. 将页面发布到 GitHub Pages。
6. 同事访问 GitHub Pages 地址后，登录账号即可保存和查看统一申请记录。

## 当前运行方式

当前配置为纯云端模式：

- 页面保存申请时，只写入 Supabase。
- 过往申请页只从 Supabase 读取记录。
- 不再调用本地 `/api` 接口。
- 不再写入本地 Excel 或 `applications.json`。

## 数据同步逻辑

- 评分页保存申请时，优先写入 Supabase。
- 过往申请页实时从 Supabase 拉取记录。
- 如果 Supabase anon public key 未配置，页面会提示配置缺失，不会把申请记录写入本地。

## Excel 导出

公网版本不直接写入本机 Excel。  
需要 Excel 时，在过往申请一览页点击“导出”，系统会根据当前云端记录生成 CSV 文件。

## 权限建议

第一版推荐所有内部账号均可查看和新增申请。  
如需区分管理员删除权限，可在 Supabase 中进一步调整 RLS policy。
