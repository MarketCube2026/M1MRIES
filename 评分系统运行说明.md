# 评分系统运行说明

## 推荐公网运行方式

公网共享建议采用 GitHub Pages + Supabase：

- GitHub Pages 负责打开网页。
- Supabase 负责保存、读取、删除和导出申请记录。
- 用户电脑关机、重启、本地服务关闭，都不会影响公网网页和云端数据。

### 配置步骤

1. 登录 Supabase 并新建项目。
2. 在 SQL Editor 中执行 `supabase_schema.sql`。
3. 在 Authentication 中创建内部同事账号。
4. 修改 `cloud-config.js`，填入 Supabase 项目地址和 anon public key，并设置：

```js
enabled: true,
authRequired: true
```

5. 将页面发布到 GitHub Pages。
6. 同事访问 GitHub Pages 地址后，登录账号即可保存和查看统一申请记录。

## 本地兼容方式

如 `cloud-config.js` 未启用云端：

- 评分页：http://localhost:8036/index.html
- 过往申请页：http://localhost:8036/history.html
- 健康检查：http://localhost:8036/api/health

启动方式：

- 双击 `启动评分系统.bat`
- 或运行 `start_excel_server.ps1`

本地方式仍会保存到：

- `applications.json`
- `资源支持价值评估评分表2026版.xlsx`

## 数据同步逻辑

- 评分页保存申请时，优先写入 Supabase。
- 过往申请页实时从 Supabase 拉取记录。
- 如果 Supabase 未配置，则回退到本地 Python 服务。
- 如果本地服务也不可用，则至少保留浏览器缓存，避免页面完全不可用。

## Excel 导出

公网版本不直接写入本机 Excel。  
需要 Excel 时，在过往申请一览页点击“导出”，系统会根据当前云端记录生成 CSV 文件。

## 权限建议

第一版推荐所有内部账号均可查看和新增申请。  
如需区分管理员删除权限，可在 Supabase 中进一步调整 RLS policy。
